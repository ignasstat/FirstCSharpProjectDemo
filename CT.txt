SELECT 
	 FileID
	,'E' AS [Source]
	,Folder
	,[FileName]
	,CreatedDate
	,UpdatedDate
	,Supplieddate
	,FileSize,
	FileExists
FROM xxx sfd
WHERE FileExists = 'Y'
UNION
SELECT 
	 DataIn_FileId
	,'I' AS [Source]
	,Folder
	,[Filename]
	,CreatedDate
	,UpdatedDate
	,SuppliedDate
	,FileSize,
	FileExists
FROM yyy
WHERE FileExists = 'Y'

AND CHARINDEX('\CallTrace\', Folder) > 0
